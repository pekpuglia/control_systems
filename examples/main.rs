// use control_systems::{systems::SecondOrder, DynamicalSystem, UnityFeedback, VecConcat};
// use nalgebra::{vector, Vector1, Vector2};
fn main() {
    // let test = SecondOrder::new(1.0, 1.0);

    // let feedback1 = UnityFeedback::<Vector1<f64>, Vector2<f64>, SecondOrder>::new_unity_feedback( test);

    // let output = feedback1.y(0.0, &VecConcat::new(vector![0.0, 1.0], vector![]), &vector![1.0]);

    // println!("{}", output);
}